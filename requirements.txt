# Runtime dependencies:
# required to run the detector CLI, the PandaData point-in-time provider,
# the rule engine and the JSON/Parquet materializers.
# Install with: python -m pip install -r requirements.txt
panda_data==0.0.12
pandas>=2.0,<4
numpy>=1.24,<3
pyarrow>=14,<26
PyYAML>=6,<7
