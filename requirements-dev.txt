# Development / test dependencies:
# used for local development, tests and CI validation. Includes the runtime
# requirements and adds pytest.
# Install with: python -m pip install -r requirements-dev.txt
-r requirements.txt
pytest>=8,<9
